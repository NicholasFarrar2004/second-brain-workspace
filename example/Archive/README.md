# Archive boundary

This folder is reserved for completed, inactive projects. No project has been moved here in the fictional example.

An inactive project's history may be useful, but it should not silently enter an unrelated active route. The example route configuration selects active project context explicitly. The validator checks links but does not archive or restore projects.
